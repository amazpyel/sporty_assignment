# Single Bet Placement Feature Execution Results

## Execution summary
| Test Case ID | Title | Steps | Expected Result | Priority | Result | Issues |
|---|---|---|---|---|---|---|
| **TCSBP-01** | Place valid single bet | Preconditions:<br>Call POST /api/reset-balance, then GET /api/balance and record the balance as B0.<br><br>1. On match M, click the 1 (home) odds button and note the odds O.<br>2. Enter a stake of `10.00`.<br>3. Check that the slip shows the selection, stake, balance B0 and payout 10.00 × O.<br>4. Click **Place Bet**.<br>5. Close the receipt. | The button shows **Placing...** and then succeeds.<br>The receipt shows Bet ID, match (home vs away), selection, stake €10.00, odds O, payout €10.00 × O and a timestamp.<br>After closing, the slip is empty and the balance is **B0 − €10.00**. | **Critical** | ![Fail](https://img.shields.io/badge/-Fail-red) | [SBP-2](issues/issues.md#sbp-2), [SBP-3](issues/issues.md#sbp-3), [SBP-11](issues/issues.md#sbp-11) |
| **TCSBP-02** | Accept maximum permitted stake of €100.00 | Preconditions:<br>Call POST /api/reset-balance, then GET /api/balance and record the balance as B0 (≥ €100.00).<br><br>1. Select an upcoming match and a valid outcome.<br>2. Enter `100.00` as stake.<br>3. Click **Place Bet**. | No validation message is shown.<br>The bet is placed successfully.<br>The balance is **B0 − €100.00**. | **Medium** | ![Fail](https://img.shields.io/badge/-Fail-red) | [SBP-2](issues/issues.md#sbp-2) |
| **TCSBP-03** | Reject stake exceeding maximum by €0.01 (UI) | Preconditions:<br>Call POST /api/reset-balance, then GET /api/balance and record the balance as B0.<br><br>1. Select an upcoming match and a valid outcome.<br>2. Enter `100.01` as stake.<br>3. Attempt to place the bet. | The message **Maximum stake is €100.00** is shown.<br>Placement is blocked, and no request is sent.<br>The balance remains **B0**. | **High** | ![Pass](https://img.shields.io/badge/-Pass-brightgreen) | None |
| **TCSBP-04** | Reject stake exceeding available balance by €0.01 (API) | Preconditions:<br>Call POST /api/reset-balance. Place a €100.00 bet so that the balance is below €100.00. Call GET /api/balance and record it as B.<br><br>1. Send POST /api/place-bet with a valid matchId and selection and `stake: B + 0.01`.<br>2. Call GET /api/balance. | The API returns **422** with an insufficient-balance error.<br>No bet is created.<br>The balance remains **B**. | **Critical** | ![Fail](https://img.shields.io/badge/-Fail-red) | [SBP-1](issues/issues.md#sbp-1) |
| **TCSBP-05** | Reject bet on past match (API) | Preconditions:<br>Call POST /api/reset-balance, then GET /api/balance and record the balance as B0.<br>Call GET /api/matches and identify a match M with kickoffDate before today.<br><br>1. Send POST /api/place-bet with the matchId of M, a valid selection and `stake: 10`.<br>2. Call GET /api/balance.<br><br>Note: the exact error code will be updated after SBP-6 is clarified. | The API rejects the request (non-200).<br>No bet is created.<br>The balance remains **B0**. | **Critical** | ![Fail](https://img.shields.io/badge/-Fail-red) | [SBP-4](issues/issues.md#sbp-4), [SBP-6](issues/issues.md#sbp-6) |
| **TCSBP-06** | Reject stake below minimum (€0.99) (API) | Preconditions:<br>Call POST /api/reset-balance, then GET /api/balance and record the balance as B0.<br><br>1. Send POST /api/place-bet with a valid matchId and selection and `stake: 0.99`.<br>2. Call GET /api/balance. | The API returns **422** with a minimum-stake error.<br>No bet is created.<br>The balance remains **B0**. | **High** | ![Pass](https://img.shields.io/badge/-Pass-brightgreen) | None |
